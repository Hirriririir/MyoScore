% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_radar.R
\name{myoscore_plot_radar}
\alias{myoscore_plot_radar}
\title{Radar Chart of MyoScore Dimensions}
\usage{
myoscore_plot_radar(
  scores,
  groups = NULL,
  colors = NULL,
  facet = TRUE,
  title = NULL,
  show_values = TRUE,
  ...
)
}
\arguments{
\item{scores}{A data.frame from \code{\link[=myoscore_score]{myoscore_score()}}, or a named numeric
vector of length 5 (one value per dimension), or a matrix/data.frame
where each row is a group and columns are dimension scores.}

\item{groups}{Optional. A factor or character vector assigning each row
of \code{scores} to a group. When provided, group means are plotted.}

\item{colors}{Optional. Character vector of colors (one per group).
Default uses \code{\link[=myoscore_colors]{myoscore_colors()}} with type \code{"spectrum"}.}

\item{facet}{Logical. If \code{TRUE} and multiple groups exist, plot
each group in a separate panel. Default \code{TRUE}.}

\item{title}{Optional main title.}

\item{show_values}{Logical. Show score values at vertices. Default \code{TRUE}.}

\item{...}{Additional arguments passed to \code{fmsb::radarchart()}.}
}
\value{
Invisible \code{NULL}. Called for its side effect (plot).
}
\description{
Plot a radar (spider) chart showing the five MyoScore dimensions.
Supports plotting one or more groups (e.g., disease stages) as
overlaid or faceted panels.
}
\details{
Requires the \pkg{fmsb} package (in Suggests).
}
\examples{
\dontrun{
scores <- myoscore_score("counts.csv")
# Single radar for all samples (mean)
myoscore_plot_radar(scores)

# Grouped by condition
myoscore_plot_radar(scores, groups = metadata$condition)
}
}
